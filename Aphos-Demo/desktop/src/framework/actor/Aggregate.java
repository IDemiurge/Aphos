package framework.actor;

/**
 * Created by Alexander on 11/5/2023
 * Separate UI and Sprites more solidly ? With aggregation we could then mix later
 */
public class Aggregate {
    //aggregate objects that will define drawing strategy and such
    /*
    things that I should've aggregated in Eidolons:
    >> All 'behaviors' (much to learn from Unity here!)
    >> All overlays (instead of defining endless containers!)
    >>
     */
}
